<h1> <%= User.username %> </h1>

<dl>
  <dt>ID</dt>
  <dd> <%= User.id %> </dd>

  <dt>Edit user</dt>
  <dd>
    <form action="/update_user/:id" method="post">
      <label for="browser_username">Username</label>
      <input id="browser_username" type="text" name="input_username" value=anisa>

      <button>Update user</button>
    </form>
  </dd>
</dl>

<h2>Own photos ( <%= User.own_photos.count %> )</h2>

<table border="1">
  <tr>
    <th>Image</th>
    <th>Owner</th>
    <th>Caption</th>
    <th>Posted</th>
    <th>
    </th>
  </tr>

</table>
